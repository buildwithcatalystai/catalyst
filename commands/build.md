---
description: Build a new app on Catalyst from a natural-language description.
argument-hint: "What to build (e.g. 'a logistics tracker for last-mile drivers')"
---

Use the Catalyst skill to build the app described in `$ARGUMENTS`.

Start with `mcp__catalyst-mcp__ensure_auth` to verify authentication, then follow the Catalyst skill playbook: health check, start build, brainstorm Q&A, confirm, and proceed through code generation.
